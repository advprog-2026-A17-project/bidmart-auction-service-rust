# Auction service local env
# Local repo values are used first, then bidmart-infrastructure/.env if present

# Server
BIND_ADDRESS=0.0.0.0:8082

# Database
# Production on Heroku should use a Supabase Postgres URL:
# DATABASE_URL=postgresql://postgres.<project-ref>:<password>@<supabase-host>:5432/postgres?sslmode=require
# For local dev you can use sqlite or Postgres. Production should use Postgres.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/bidmart_auction

# Downstream services
# Production auth/frontend are live, but auction depends on catalogue and wallet.
# Keep these pointed at the final live catalogue/wallet URLs once your devmates finish them.
CATALOGUE_SERVICE_URL=http://localhost:8081
# Optional gRPC catalogue endpoint (HTTP used when both are set and gRPC unset)
# CATALOGUE_GRPC_URL=http://127.0.0.1:50051
WALLET_SERVICE_URL=http://localhost:8083
# Optional gRPC wallet endpoint (HTTP preferred when both are set)
# WALLET_GRPC_URL=http://127.0.0.1:50053

# RabbitMQ outbox publisher
RABBITMQ_URL=amqp://guest:guest@localhost:5672/%2f
BIDMART_EVENTS_EXCHANGE=bidmart.events

# Internal service authentication
GATEWAY_INTERNAL_TOKEN=bidmart-local-internal-token

# Schedulers (milliseconds)
AUCTION_CLOSURE_SCHEDULER_INTERVAL_MS=1000
OUTBOX_SCHEDULER_INTERVAL_MS=1000

# Bid hold grace after auction end (seconds, default 72h)
BID_HOLD_GRACE_SECONDS=259200

# Swagger UI (docker-compose)
SWAGGER_UI_PORT=8088
